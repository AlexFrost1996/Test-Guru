<nav class="navbar navbar-light bg-light">
  <%= link_to "TestGuru", root_path, class: 'navbar-brand' %>

  <span class="navbar-text">

    <% if user_signed_in?%>
      <% if current_user.admin?%>
        <button type="button" class="btn btn-link mr-19">
          <%= link_to "Admin Panel", admin_tests_path %>
        </button>
      <% end %>
      <%= link_to 'Badges', badges_path, class: "btn btn-link mr-19" %>
      Welcome, <b><%= current_user.email %></b> Guru |
      <%= link_to 'Log out', destroy_user_session_path, method: :delete %>
      <% else %>
        <%= link_to "Long in", new_session_path(resource_name) %> |
        <%= link_to "Sign up", new_registration_path(resource_name) %>
    <% end %>
  </span>
</nav>
